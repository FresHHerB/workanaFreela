# Frontend Environment Variables
# These are used by Vite during build and runtime

# Supabase Configuration for Frontend
# Required: Database connection for the React dashboard
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Instructions:
# 1. Copy this file to .env in the same directory
# 2. Fill in your actual Supabase credentials
# 3. These will be used by the React frontend to connect to the database

# Note:
# - Variables must start with VITE_ to be accessible in React
# - These values will be embedded in the built frontend
# - Use only public/anon keys, never private keys