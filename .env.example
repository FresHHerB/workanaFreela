# Workana Authentication
# Required: Your Workana login credentials
WORKANA_EMAIL=your_email@example.com
WORKANA_PASSWORD=your_password

# Supabase Configuration
# Optional: Database connection for storing scraped data
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key

# Server Configuration
# Optional: Server settings (defaults provided)
PORT=8000
HOST=0.0.0.0
DEBUG=false

# Instructions:
# 1. Copy this file to .env
# 2. Fill in your actual Workana credentials
# 3. Configure Supabase if using database storage
# 4. Adjust server settings if needed

# Security Notes:
# - Never commit .env files to version control
# - Use strong passwords and keep credentials secure
# - Supabase keys should have minimal required permissions