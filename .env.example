# =============================================================================
# WORKANA FREELA - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values

# -----------------------------------------------------------------------------
# BACKEND - WORKANA SCRAPING (Required)
# -----------------------------------------------------------------------------
# Your Workana login credentials for web scraping
WORKANA_EMAIL=your_email@example.com
WORKANA_PASSWORD=your_password

# -----------------------------------------------------------------------------
# FRONTEND - SUPABASE DATABASE (Required)
# -----------------------------------------------------------------------------
# Database connection for the React dashboard to read scraped data
# These variables are used only by the frontend during build and runtime
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...your_key_here

# -----------------------------------------------------------------------------
# WEBHOOK INTEGRATION (REQUIRED for frontend button)
# -----------------------------------------------------------------------------
# External webhook URL that triggers scraping (usually n8n webhook)
# Used by the "Atualizar Dados" button in the frontend
VITE_WEBHOOK_URL=https://your-n8n-webhook.com/webhook/update_freela

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# Server settings - defaults are usually fine
PORT=8000
HOST=0.0.0.0
DEBUG=false

# -----------------------------------------------------------------------------
# EASYPANEL DEPLOYMENT
# -----------------------------------------------------------------------------
# Configure these variables in EasyPanel environment settings:
#
# REQUIRED:
# 1. WORKANA_EMAIL=your_email@workana.com
# 2. WORKANA_PASSWORD=your_password
# 3. VITE_SUPABASE_URL=https://your-project.supabase.co
# 4. VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
# 5. VITE_WEBHOOK_URL=https://your-n8n-webhook.com/webhook/update_freela (REQUIRED!)
#
# OPTIONAL:
# 6. PORT=8000 (defaults to 8000 if not set)

# -----------------------------------------------------------------------------
# ARCHITECTURE NOTES
# -----------------------------------------------------------------------------
# BACKEND: Workana credentials for scraping + saves to Supabase
# FRONTEND: Reads data from Supabase database
# WEBHOOK: External trigger (n8n) calls /api/scrape endpoint
#
# Flow: Webhook/n8n → /api/scrape → Backend scrapes → Saves to Supabase → Frontend reads

# -----------------------------------------------------------------------------
# SECURITY NOTES
# -----------------------------------------------------------------------------
# - Never commit .env files to version control
# - Use strong passwords and keep credentials secure
# - Supabase anon keys should have minimal required permissions
# - Only share credentials with authorized team members