# =============================================================================
# WORKANA FREELA - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# All variables below are used by both backend and frontend

# -----------------------------------------------------------------------------
# WORKANA AUTHENTICATION (Required)
# -----------------------------------------------------------------------------
# Your Workana login credentials for scraping
WORKANA_EMAIL=your_email@example.com
WORKANA_PASSWORD=your_password

# -----------------------------------------------------------------------------
# SUPABASE DATABASE (Required)
# -----------------------------------------------------------------------------
# Database connection for storing and retrieving scraped data
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...your_key_here

# Frontend also uses these with VITE_ prefix (handled automatically)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...your_key_here

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# Server settings - defaults are usually fine
PORT=8000
HOST=0.0.0.0
DEBUG=false

# -----------------------------------------------------------------------------
# EASYPANEL DEPLOYMENT INSTRUCTIONS
# -----------------------------------------------------------------------------
# When deploying to EasyPanel, set these environment variables:
#
# 1. WORKANA_EMAIL: Your Workana login email
# 2. WORKANA_PASSWORD: Your Workana password
# 3. SUPABASE_URL: Your Supabase project URL
# 4. SUPABASE_ANON_KEY: Your Supabase anonymous key
# 5. VITE_SUPABASE_URL: Same as SUPABASE_URL (for frontend)
# 6. VITE_SUPABASE_ANON_KEY: Same as SUPABASE_ANON_KEY (for frontend)
# 7. PORT: 8000 (optional, this is the default)
#
# Note: The VITE_ prefixed variables are required for the React frontend
# to access Supabase during build time and runtime.

# -----------------------------------------------------------------------------
# SECURITY NOTES
# -----------------------------------------------------------------------------
# - Never commit .env files to version control
# - Use strong passwords and keep credentials secure
# - Supabase anon keys should have minimal required permissions
# - Only share credentials with authorized team members